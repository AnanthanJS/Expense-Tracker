import React from 'react';
import ExpenseManager from '../components/ExpenseManager';

const Home = () => (
  <div>
    <h1>Expense Tracker</h1>
    <ExpenseManager />
  </div>
);

export default Home;
